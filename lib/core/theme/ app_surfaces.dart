import 'package:flutter/material.dart';
import 'app_colors.dart';

class AppSurfaces {
  /// Card surface over gradient background
  //static Color card = AppColors.purpleDark.withOpacity(0.85);
  static Color card = AppColors.card;

  /// Elevated card (slightly stronger)
  static Color cardElevated = AppColors.purpleDark.withOpacity(0.92);

  /// Glass / bottom nav base
  static Color glass = AppColors.deep2.withOpacity(0.6);
}
